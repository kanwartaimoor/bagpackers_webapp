<%= form_with(model: car_rental, local: true) do |form| %>
  <% if car_rental.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(car_rental.errors.count, "error") %> prohibited this car_rental from being saved:</h2>

      <ul>
        <% car_rental.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
