<%= form_for([:dashboard,tour_detail.tour,tour_detail], local: true, remote: true) do |form| %>
  <div class="direction-r" style="margin-top: 50px" id = "tour-details-<%= tour_detail.id %>">
    <div class=flag-wrapper"">
      <h3>Day Number <%= tour_detail.day + 1 %></h3>
    </div>
    <div class="flag-wrapper">
      <span class="flag"><%=  form.label :location %> </span>
      <span class="flag"><%=  form.text_field :location %> </span>
    </div>
    <div class="desc">
      <p><%= form.label :description %></p>
    <p><%= form.text_area :description %></p>
    </div>
    <div class="desc">
      <p><%= form.submit "Update" %></p>
    </div>
  </div>
<% end %>