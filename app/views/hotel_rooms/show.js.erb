$('div[id^="hotel-review-"]').remove();
<%= @hotel_reviews.each do |review| %>
$("#review_box").html("<%= escape_javascript(render  "hotel_reviews/hotel_review", review: review) %>");
<% end %>
$("#hotel_review_paginate").html("<%= j js_will_paginate @hotel_reviews %>");